import {StringKeyObject} from '@app-types'
import React from "react";

export default function useRefsList(nameList: Array<string>)  {
    // let result: StringKeyObject = {};

    // for (var i = 0; i < nameList.length; i++) {
    //     console.log(i);
    //     result[nameList[i]] = React.useRef();
    //  }
    // return result;
}